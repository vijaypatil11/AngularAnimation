import { NotwidthDirective } from './notwidth.directive';

describe('NotwidthDirective', () => {
  it('should create an instance', () => {
    const directive = new NotwidthDirective();
    expect(directive).toBeTruthy();
  });
});
